services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_URL: ""
    container_name: kairos
    restart: unless-stopped
    ports:
      - "80:80"
    env_file:
      - .env